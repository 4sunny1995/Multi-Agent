syntax = "proto3";

package microservice;

// Định nghĩa dịch vụ mẫu
service ExampleService {
  rpc GetData (Request) returns (Response) {}
}

message Request {
  string id = 1;
}

message Response {
  string data = 1;
  int32 status = 2;
}